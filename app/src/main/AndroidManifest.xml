<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.geancarloleiva.a4_listandrecyclerview">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_shop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_shop"
        android:supportsRtl="true"
        android:theme="@style/Theme.4_ListAndRecyclerView"
        tools:targetApi="31">
        <activity
            android:name=".controller.ProductActivity"
            android:exported="false" />
        <activity
            android:name=".controller.CategoryRecyclerViewActivity"
            android:exported="false" />
        <activity
            android:name=".controller.CategoryListViewActivity"
            android:exported="false" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>